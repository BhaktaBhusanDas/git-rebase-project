{
  "name": "jenkins-automation-project",
  "version": "1.0.0",
  "description": "Enterprise Jenkins automation pipeline",
  "main": "index.js",
  "scripts": {
    "build": "webpack --mode=development",
    "build:prod": "webpack --mode=production",
    "test": "jest",
    "deploy": "./src/scripts/deploy.sh"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "jest": "^29.6.0"
  }
}
